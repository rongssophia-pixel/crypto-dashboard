# Ingestion Service Dependencies
# Simplified for automatic ingestion

# Core framework
fastapi
uvicorn[standard]

# Kafka
confluent-kafka>=2.3.0

# Data validation
pydantic
pydantic-settings

# HTTP clients
aiohttp
websockets

# Monitoring
prometheus-client

# Utilities
python-dateutil

# Development tools
flake8
black
isort
pre-commit

# Testing
pytest
pytest-asyncio

# Note: Removed dependencies:
# - grpcio/grpcio-tools/protobuf (no longer using gRPC)
# - psycopg2-binary/sqlalchemy (no longer using PostgreSQL for stream tracking)
# - ccxt (using native Binance WebSocket API)
# - python-jose/passlib (JWT auth handled by API gateway)
# - httpx (using aiohttp only)
